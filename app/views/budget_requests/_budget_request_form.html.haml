%fieldset.inputs
  %ol
    = form.input :request_id, :as => :hidden
    = form.input :name, :label => "Budget Item"
    = form.input :amount_requested, :label => "Grant Amount Requested"
    = form.input :amount_recommended, :label => "Grant Amount Recommended"
    = render :partial => "budget_requests/extra_budget_request_form", :locals => {:model => model, :form => form}
    = form.commit_button
