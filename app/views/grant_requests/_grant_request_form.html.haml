- if model.granted && params[:amend]
  = render :partial => "grant_request_amend_form", :locals => { :model => model, :form => form }
- else
  = render :partial => "grant_request_normal_form", :locals => { :model => model, :form => form }
